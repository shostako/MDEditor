# 変更履歴

MDEditor のバージョンごとの変更点。新しいものが上。

バージョンの対応は `app/build.gradle.kts` の `versionCode` / `versionName` の変更履歴から起こしている。一部のバージョンはバージョン更新が commit されないまま APK だけ配布されており、内容を追えない（該当箇所に明記した）。

---

## 1.14 (code 19) — 2026-08-09

### 修正

- **Activity 再生成でバックスタックが消える問題を解消。** 放置後に「戻るボタンが効かない」症状の原因。画面消灯やダークモードの時刻切替で Activity が再生成されると、スプラッシュ表示のために `MDEditorApp` がコンポジションから外れ、`NavController` が作り直されてバックスタックが startDestination の1件だけになっていた。スプラッシュを本体の上に重ねる構造に変更し、本体を常時コンポーズするようにした
- インデックス走査が呼び出し元の画面寿命に縛られていた問題を修正。`VaultIndex` に専用スコープを持たせ、画面を離れても走査が完走するようにした。従来は途中で中断され、`syncFull` が走査完了後にまとめて DB へ書く作りのため**何も書かれずエラーも出ない**状態だった
- 再インデックス中断時に `isSyncing` が `true` のまま固着し、アプリを再起動するまでスピナーが戻らない問題を修正

## 1.13 (code 18) — 2026-08-09

### 追加

- **ファイル一覧の表示フィルタ。** 既定でフォルダ・ノート(`.md`)・画像だけを表示する。従来は Drive の子を全件出していたが、タップが効くのはフォルダと `.md` だけで、それ以外は押しても反応しない行として並んでいた
- ドット始まり（`.obsidian/`、`.trash/` 等）を一覧から除外
- 隠した件数を一覧末尾に「他 N 件（表示対象外）」として表示。絞り込みの結果ゼロになったフォルダは「空のフォルダ」と区別して表示する
- 設定に「すべてのファイルを表示」スイッチを追加
- **一覧の画像をタップして開けるようにした。** 従来はエディタ本文の `![[画像.png]]` からしか開けなかった

### 修正

- ドット始まりフォルダを検索インデックスからも除外。`.trash/` の削除済みノートが、一覧には出ないのに検索では現役ノートと同じ見た目で出てきて、開いて編集・保存までできていた
- `.md` の判定が mimeType 依存だったため、`application/octet-stream` で保存されたノートが一覧からも検索からも消える穴を修正
- 画像ビューアがデコードできない形式（SVG 等）でローディングスピナーのまま止まる問題を修正
- 画像ビューアのデコードに上限（最長辺 4096px）を設定。高解像度写真を原寸デコードして OOM になるのを防ぐ
- TTS バーの停止ボタンを再生/一時停止の右隣へ移動（並びを `⏮ ⏸ ⏹ ⏭ 速度` に変更）

## 1.12 (code 17) — 2026-07-22

### 修正

- 暗号化認証ストレージ破損時の起動クラッシュを解消。端末修理等で Keystore と `EncryptedSharedPreferences` が不整合になっても、認証情報だけリセットして起動を継続する（Drive 上のデータには触れない）

## 1.11 (code 16) — 2026-07-22

### 追加

- エディタに兄弟ノート送り（前/次）。同フォルダのノートを一覧と同じ並びでめくる

## 1.10 — 2026-06-12

APK は配布されているが、バージョン更新が commit されていないため変更内容を追えない。

## 1.9 (code 14) — 2026-06-11

### 追加

- ノートのアウトライン（見出しジャンプ）

## 1.8 (code 12) — 2026-06-11

### 変更

- TTS を見出し単位のステップナビゲーション方式へ転換

## 1.7.2 (code 11) — 2026-06-11

### 追加

- ノートの音声読み上げ（TTS）

## 1.6 / 1.7 / 1.7.1 — 2026-06-10 〜 06-11

APK は配布されているが、バージョン更新が commit されていないため変更内容を追えない。

## 1.5 (code 7) — 2026-06-10

### 追加

- 編集モードで frontmatter を生 YAML として編集可能に

## 1.4.1 (code 6) — 2026-06-10

### 修正

- 編集モードでも frontmatter パネルを表示するように

## 1.4 (code 5) — 2026-06-10

### 追加

- frontmatter プロパティの表示トグル

## 1.3 (code 4) — 2026-06-02

### 追加

- **LaTeX 数式表示。** `$$...$$` / `$...$` を JLatexMath で描画（Markwon ext-latex）

### 修正

- `MathNormalizer` の内部トークンから NUL バイトを除去

## 1.2 (code 3) — 2026-05-25

### 修正

- Linkify の autoLink を URL / email のみに限定

## 1.1 (code 2) — 2026-05-25

### 修正

- Markwon の文字色・リンク色を `MaterialTheme` から動的に注入（ダークモード追従）

## 1.0 (code 1) — 2026-05-25

初回リリース。M0–M12 のスナップショット。

- Google Drive API 直叩きによる Obsidian Vault の読み書き（AppAuth + Retrofit、SAF・Google API Client 不使用）
- Markdown プレビュー（Markwon: テーブル / 打ち消し線 / タスクリスト / HTML）
- Obsidian Wikilink（`[[ノート]]`、`[[note|alias]]`、`![[画像.png]]`）とノート間ジャンプ
- 全文検索（ファイル名 + 本文、Room 永続化 + Drive changes API 差分同期）
- 編集・保存（Drive へ直接 PATCH、未保存変更の破棄確認）
- YAML frontmatter の分離表示
- ダークモード
- release 用 keystore 署名対応
